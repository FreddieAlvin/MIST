# Core RL Framework
gymnasium==0.29.1
stable-baselines3[extra]>=2.1.0
shimmy>=1.3.0  # Required for compatibility between Gym and Gymnasium

# Numerical & Data Processing
numpy<2.0.0    # Recommended for compatibility with older RL libraries
pandas
matplotlib
scipy

# Computer Vision (for the NatureCNN and Camera processing)
opencv-python
torch>=2.0.0
torchvision

# Webots Integration (If using the webots_ros2 or specific wrappers)
# Note: The 'controller' library is usually provided by the Webots installation itself